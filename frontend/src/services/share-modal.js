// Au début du fichier
document.addEventListener('DOMContentLoaded', function() {
  const element = document.getElementById('votreElement'); // Remplacez par le bon ID
  if (element) {
    element.addEventListener('click', function() {
      // Votre code
    });
  }
});